{
  "name": "artesania-gualeguay-tests",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "jest --config jest.config.js --testEnvironment jsdom",
    "lint": "eslint js/ tests/ --ext .js",
    "format": "prettier --write \"**/*.{js,json,css,html}\"",
    "lint:fix": "eslint js/ tests/ --ext .js --fix",
    "e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "eslint": "^8.57.1",
    "eslint-config-prettier": "^9.1.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "playwright": "^1.61.1",
    "prettier": "^3.0.0"
  }
}
